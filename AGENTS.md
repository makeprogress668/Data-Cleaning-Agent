# AGENTS.md

给在这个仓库里工作的 AI 助手。**只写代码里读不出来的东西** —— 目录结构看
`docs/PROJECT_STRUCTURE.md`，怎么跑看 `README.md`。这里写的是为什么这么做，
以及踩过哪些坑。

## 这个产品的一条原则

**按用户要求操作数据，没要求的事情不做。**

去重、删空值、清洗 —— 用户没说，就保留原数据。用户没问，就不要主动上报问题。
唯一的例外是用户明确要求「帮我分析这个文件」。

这不是一条风格偏好，它决定了几乎每个分支的默认值。**判断不准的时候，失误的方向
必须是「停下来问」，不能是「擅自动手」，也不能是「装作没听见」。**

## 环境

用 `backend/.venv/Scripts/python.exe`，不要用系统 Python。两个解释器都能跑起来，
但依赖版本不同 —— 曾经出现过线上服务跑在系统 Python 上、而测试跑在 venv 里的情况。

```bash
backend/.venv/Scripts/python.exe -m uvicorn data_agent.api:app --host 127.0.0.1 --port 8000
```

`pandas<3.0` 是刻意锁的。pandas 3 改变了缺失值语义（object 列 ffill 得到 NaN 而非
None），会静默改变导出的单元格值。升级是一项独立任务，不是版本号 bump。

## 改动之前必须知道的四件事

（命令、架构和各能力的位置见 `CLAUDE.md`。）

### 1. 破坏性动作靠「引用授权」，不靠动词表

删行、去重这类动作是否执行，取决于能不能**引用用户目标里的原话**
（`planning/provenance.py`）。动词表（`_KEEP_VERBS` / `wants_deduplication` 等）
降级成了「第二条准许通道」—— 它只能准许，不能否决。

**不要往动词表里加词来修 bug。**那正是拆掉的旧边界：表里漏一个说法，后果不是
「没听懂」，而是掉进错误的分支。真正的修法是让模型引用原话。

### 2. 执行器做的每件事都必须在能力注册表里

`capabilities/builtins.py` 是完整白名单。运行器记录的每个阶段要能对应到计划里的
一个步骤，靠 `capability_id` 匹配。

漏注册的后果是静默的：阶段的指标在投影时被丢弃，而计划里对应的步骤被报成
`skipped` —— 遥测开始说假话。`union_tables` 就这么漏过一次。

### 3. 数值解析只用于计算，绝不改数据

`utils/numeric.py` 把 `1,234`、`(567)`、`1.2万` 读成数字，是为了让汇总算得对。
**交付的明细里这些单元格必须保持用户上传时的样子。**顺手「规范」一下就违反了
上面那条原则。

### 4. 单元测试全绿不等于交付物是对的

单测全绿的时候，「按城市统计金额合计」照样交出过一张全是 0 的汇总表。

改任何影响交付物的逻辑，跑验收矩阵：

```bash
python scripts/run_acceptance_matrix.py scripts/acceptance_cases.json --no-llm
```

它检查的是**用户拿到的工作簿**，不是函数行为。CI 也跑这一条。
需要模型才能通过的用例标了 `requires_llm`，`--no-llm` 下跳过。

## 契约链

```
TaskSpec → JobConfig → ExecutionPlan → PlanSnapshot → OutputSpec
```

改一层就要看下一层是否跟得上。真实踩过的：

- TaskSpec 产出了汇总规格，却没声明 `analyze` 动作 → 覆盖校验拒绝了计划自己
  产出的步骤
- 合并同结构表之后，被并掉的表名消失，而 `base_table` 正指向它 → 计划引用了一张
  不存在的表

`OutputSpec` + Output Linter 决定工作簿里能出现哪些 sheet 和字段。没声明的写不进去。

## 语言

面向用户的每一个字都是中文，而且必须是业务语言。`duplicate_订单号` 这种内部记号
不许出现在工作簿里 —— 翻译集中在 `tools/issue_wording.py`，它不读 DataFrame，
改措辞不需要看懂审计表怎么构建。

代码注释和 docstring 用英文，解释**为什么**，特别是当前写法避免了什么问题。

## 已知的坑

- **理解缓存默认开启**。相同目标 + 相同表结构会复用上次的理解。调提示词时它会让
  你以为改动没生效 —— `data-agent understanding-cache --clear`。
- **加澄清问题之前先想清楚它该不该拦**。只有 `priority="high"` 的槽位会阻塞任务。
  判断标准是「没有这个答案还能不能交付用户要的东西」：「能用的留下」没说清什么算
  能用，不问就交付全部行 —— 该拦；「括号是不是负数」有标准答案 —— 不该拦，按默认
  做并在结果里说明。模型给的问题带 `blocking` 标记，确定性层只负责尊重它。
- **澄清一律是选择题，候选来自这次上传的数据**。见
  `planning/clarification_options.py`。能写出「按 status 字段保留 paid」的用户根本
  不需要被问；给空白框他答得含糊，给按钮他答得准。
- **理解侧仍然是词表驱动的**。「列出关联不上的记录」这类说法确定性层认不出（有一条
  正面测试钉着这个缺口）。漏词的后果已经不是删错数据，而是「我说了你没做」。修法
  不是往词表里加词 —— 见上面第 1 条。
